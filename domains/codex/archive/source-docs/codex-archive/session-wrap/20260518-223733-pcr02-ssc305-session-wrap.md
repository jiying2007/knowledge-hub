# pcr02_ssc305_compile 会话归档

## Source

- Date: 2026-05-18
- Project: pcr02_ssc305_compile
- Scope: 当前 Codex 会话
- Repository Branch: robot_pcr02
- Final Commit: fb5994d31 refactor(build): 统一构建与发布流程
- Archive Type: session-wrap

## 本次完成

- 建立根目录统一构建入口 `build.sh`，覆盖应用构建、系统镜像、OTA/SD 打包、自检、工具链检测、源码状态检查、模块命令等团队常用流程。
- 新增 `scripts/generate-build-info.sh`，统一生成构建版本信息，修复旧版本脚本无法稳定识别最新 tag 的问题，并支持轻量 `--self-check`。
- 重写团队使用指南 `README.md`，明确首次准备、标准构建、快速编译、仅打包 OTA/SD、自动化构建、源码同步、故障排查和 GROS 过渡策略。
- SDK 构建边界硬切：根构建入口不再依赖旧 `compile_*.sh`、`release.sh`、`gen_version.sh`、`SourceCode/project/xcrz_env_set.sh`、`SourceCode/sdk/verify/makefile`。
- 更新 `SourceCode/sdk/sdk.mk`，避免 SDK 主构建递归进入旧 verify 入口，应用构建由根 `build.sh` 接管。
- 移除 USB 工厂包生成路径，设备无 USB 场景下仅保留 SD/OTA/all 打包能力。
- 加入 `.toolchains/` 和 GROS 过渡目录忽略规则，避免工具链和独立应用框架产物污染主仓。
- 完成主仓提交并推送到远端：`fb5994d31 refactor(build): 统一构建与发布流程`。

## 关键决策

- 当前阶段应用根目录检测策略为 `xcrz_sigmastar_demo` 优先，缺失时再使用 `gros`；后续切换时可调整优先级。
- `modules-status` 和 `modules-sync` 为 GROS 专用命令：当 GROS 目录存在时优先选择 GROS，普通构建仍按当前阶段规则选择 xcrz。
- 工具链不固化进主仓，使用本地 `.toolchains/` 目录承载外部工具链仓拉取结果，主仓只记录使用方式和检测逻辑。
- 自动化构建不在脚本内隐式 checkout/pull；由 CI 或发布操作者先同步源码，`build.sh` 负责检测源码状态和记录 commit 证据。
- 不兼容旧入口，边界硬切并清理残留脚本，避免团队继续使用分叉且行为不一致的构建路径。
- 设备无 USB，因此不再生成 USB 工厂包；`--pack usb` 应作为非法参数失败。

## 涉及文件 / 模块

- `build.sh`
- `scripts/generate-build-info.sh`
- `README.md`
- `.gitignore`
- `SourceCode/sdk/sdk.mk`
- `SourceCode/project/pcr02_customer/brcmfmc_start.sh`
- 删除旧入口：`compile_*.sh`、`release.sh`、`gen_version.sh`、`SourceCode/project/xcrz_env_set.sh`、`SourceCode/sdk/verify/makefile`
- 删除根目录旧 `xcrz_sigmastar_demo` 链接，应用仓以 `SourceCode/sdk/verify/xcrz_sigmastar_demo` 为准

## 验证情况

- `rtk git fetch --prune`：提交前同步远端引用，当前分支无落后远端。
- `bash -n build.sh` 和 `bash -n scripts/generate-build-info.sh`：脚本语法检查通过。
- `rtk bash build.sh self-check --allow-dirty --no-copy-nfs`：自检通过，输出 main/app commit、应用根目录、工具链目录、GROS/xcrz 检测状态。
- `rtk git diff --check` 和 `rtk git diff --cached --check`：无 whitespace 问题。
- `rtk bash build.sh full --profile ap6303bh_512m_v20 --allow-dirty --no-copy-nfs`：完整构建通过，覆盖应用、系统镜像和 OTA。
- USB 负向验证：`build.sh package --pack usb` 按预期失败，确认不会生成 USB 工厂包。
- `rtk git status --short --branch`：主仓最终干净，`robot_pcr02` 与 `origin/robot_pcr02` 同步。

## 已知状态 / 恢复风险

- 嵌套应用仓 `SourceCode/sdk/verify/xcrz_sigmastar_demo` 仍有未提交变更，包含 `.gitignore` 和多份静态/动态库文件；这些不是主仓提交内容，后续如需固化，需要在应用仓单独提交和推送。
- 当前 workspace 同时存在被忽略的 `SourceCode/sdk/verify/gros` 目录和 xcrz 应用目录；普通构建选择 xcrz，GROS 专用命令选择 GROS。
- 完整构建会产生 SDK 跟踪产物噪音，本次已只恢复两个已知生成物，避免混入主仓提交。

## 下一步建议

- 若团队开始切换 GROS 为默认应用框架，下一步应调整 `build.sh` 的候选优先级，并同步 README 的当前阶段说明。
- 若要发布应用库变更，需要进入 `SourceCode/sdk/verify/xcrz_sigmastar_demo` 单独做提交前检查、提交和推送。
- CI 接入时建议固定流程为：同步主仓和应用仓，准备工具链，执行 `build.sh self-check`，执行 `build.sh full --profile <profile>`，收集版本信息和产物。
