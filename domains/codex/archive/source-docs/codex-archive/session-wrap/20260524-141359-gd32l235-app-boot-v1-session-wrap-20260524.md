# gd32l235_app_boot_v1 会话收口

## 范围

- 仓库：`~/work/mcu/gd32l235_app_boot_v1`
- 分支：`master`
- 目标：同步 `gd32l235` 构建/文档/契约基线，硬切换移除 `app-refactor-v2` 残留，继续推进 App 边界收敛和复杂流程拆分。

## 当前状态

- 工作区：干净，`rtk git status --short --branch` 输出 `## master`。
- 最新提交：
  - `fd6f790 refactor(app): 拆分电机反馈解析流程`
  - `25f9564 refactor(app): 收敛边界并拆分复杂流程`
  - `ce12cf4 chore(init): 初始化当前重构基线`

## 已完成

- 删除旧聚合头 `App/include/app/My_include.h`。
- `Tools/tests` 契约改为禁止旧聚合头回归。
- 补齐 `Tools/Vendor/toolchains` 子模块 gitlink，提交点 `caeb712d508274c6539f693ea4efe7e15a607ce3`。
- 文档状态对齐：
  - `Docs/verification/command-entrypoints.md` 增加 `git submodule update --init --recursive`。
  - `Docs/planning/drift-remediation.md` 增加 `RDR-BUILD-01`。
  - `Docs/verification/acceptance-matrix.md` 将 `.agents/`、`skills/` 状态对齐为 `ACCEPTED`。
- 已拆分并降低复杂度热点：
  - `motor_app_read_register_dual`
  - `motor_app_enter_bootloader_dual`
  - `protocol_system_try_handle_basic_command`
  - `protocol_handle_motor_hall_calibration_status`
  - `protocol_handle_motor_enter_boot`
  - `motor_parse_from_buffer`

## 验证证据

- `rtk bash scripts/codex-check.sh --quick`：PASS。
- `rtk git diff --check`：PASS。
- `rtk git diff --cached --check`：PASS。
- 跟踪文件 CRLF 扫描：`tracked_crlf_found=0`。
- 旧残留引用扫描：无 `app-refactor-v2`、`Docs/app-refactor-v2`、旧 UART 符号、`APP_ROOT_COMPAT_SRCS`、`Deprecated legacy aggregate header`、`旧契约未修复` 命中。
- `rtk bash ~/codex/scripts/final-ready.sh`：PASS，但状态 HOT，原因是上下文压力高；另有 `~/codex` 归档侧 4 个脏变更，不属于 MCU 仓库。

## 剩余热点

下一轮优先窄切，避免继续读大 diff：

1. `protocol_router.c:protocol_try_parse_frame`
2. `protocol_exchange.c:protocol_handle_motor_ota`
3. `protocol_router.c:protocol_factory_bridge_on_motor_raw_frame`
4. `motor_protocol.c:motor_app_enter_hall_calibration_dual`

## 恢复建议

- 继续使用 `repo-drift-remediation`、`adk-planning-execution-loop`。
- 若提交/PR，使用 `adk-commit-pr-quality-gate`。
- 每轮只拆一个函数，跑定向测试后跑 `rtk bash scripts/codex-check.sh --quick`。
- 所有 shell 命令必须经 `rtk`；手工文件修改必须用 `apply_patch`。
