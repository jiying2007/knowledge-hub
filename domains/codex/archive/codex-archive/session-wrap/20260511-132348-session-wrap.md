## 本次会话总结

### 已完成
- 评估了“公众号提到的 token 优化项目清单”对当前 `codex` 体系的适配性，形成并归档长期优化路线图。
- 将“回答压缩规则化”和“输出裁剪默认化”正式写入规则与手册，并明确按阶段生效，避免误伤 brainstorming、设计、计划阶段。
- 为 `usage-tail / usage-report` 补齐了 `Trim Mode`、`Likely Cause`、`attribution`，把 token 观测从“看总量”推进到“看启发式归因 + 操作建议”。
- 将 `archive-search` 升级为轻量索引检索：新增 SQLite 索引、metadata 过滤、中文回退扫描，并补充 CLI 参数与文档。
- 为 `context-preflight`、`memory-curator`、`session-wrap` 增加“自动结晶 / 未决张力”字段，形成更稳的上下文压缩与会话接力结构。
- 完成两次代码提交并推送：
  - `a9e00f2 feat(token): 收敛回答压缩与输出裁剪策略`
  - `f0c69eb feat(knowledge): 增强检索归因与会话结晶`

### 关键决策
- 不引入 `GBrain` 这类重型系统；当前阶段优先沿既有路线图做轻量闭环增强。
- 将下一阶段优化聚焦到三条线：
  - `archive-search` 索引化与 metadata 过滤
  - `usage-tail` 粗粒度归因
  - `context-*` 的自动结晶与未决 tension
- `Likely Cause` 明确定义为“启发式归因”，不伪装成精确审计。

### 自动结晶
- 省 token 的核心闭环应继续围绕：`少说 / 少读 / 可检索`，而不是继续堆更多外部系统。
- `usage-tail` 的价值已经从“面板”扩展到“工作流约束器”；高风险状态下应直接影响读取和表达策略。
- `archive-search` 只有支持 metadata 过滤和本地索引后，才能真正成为外部上下文层，而不只是 grep 替代。

### 未决张力 / 恢复风险
- `archive-search` 目前已可用，但中文 FTS 仍依赖回退扫描；后续若命中质量不足，需要继续优化索引策略。
- `usage-tail` 的 `Likely Cause` 还只是粗粒度启发式，没有做到按操作类型精细追踪。
- 当前工作区仍有两个旧的未跟踪归档项，未纳入本次提交：
  - `docs/archive/diag-architecture/`
  - `docs/archive/session-wrap/20260510-000000-diag-v4-hybrid-refcount-session-wrap.md`

### 涉及文件 / 模块
- `AGENTS.md`
- `src/codex-home/AGENTS.md`
- `README.md`
- `docs/codex-asset-management.md`
- `scripts/context-preflight.sh`
- `tools/codex_assets/archive_search.py`
- `tools/codex_assets/cli.py`
- `tools/codex_assets/memory_curator.py`
- `tools/codex_assets/usage_dashboard.py`
- `src/codex-home/vendor/skills/session-wrap/3.1.0/SKILL.md`

### 验证情况
- 已验证：
  - `rtk python3 -m py_compile tools/codex_assets/archive_search.py tools/codex_assets/usage_dashboard.py tools/codex_assets/cli.py tools/codex_assets/memory_curator.py`
  - `rtk bash scripts/check-skills.sh`
  - `/tmp` 下运行 `archive-search.sh`、`usage-report.sh --json`、`usage-tail.sh --once --view summary`、`curate-memory.sh --dry-run`、`context-preflight.sh`
- 未验证：
  - 尚未对更大规模归档数据做 `archive-search` 性能验证
  - 尚未对 `Likely Cause` 做长期统计回放验证

### 经验与风险
- 回答压缩必须按阶段处理，压的是噪音，不是思考密度。
- token 节流要进入工具层，否则仅靠规则文本很难稳定执行。
- 归档和记忆边界必须继续严格控制，避免 archive/memory/agent memory 自动双写。

### 下一步建议
- 优先处理两个旧未跟踪归档，决定是否单独提交。
- 若继续深挖，下一步应做：
  - `archive-search` 命中质量优化
  - `usage-tail` 更细的操作类型归因
  - `context-*` 与 `archive-search` 的回链联动
