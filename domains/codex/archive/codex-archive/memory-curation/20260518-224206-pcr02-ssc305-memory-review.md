# pcr02_ssc305_compile Memory Review

## Source

- Date: 2026-05-18
- Type: targeted-memory-review
- Source Archive: ~/codex/docs/archive/session-wrap/20260518-223733-pcr02-ssc305-session-wrap.md
- Related Commit: fb5994d31 refactor(build): 统一构建与发布流程
- Write Mode: report-only

## Stable Project Facts

- 主仓 `pcr02_ssc305_compile` 使用根目录 `build.sh` 作为统一构建入口。
- 当前阶段普通应用构建优先选择 `SourceCode/sdk/verify/xcrz_sigmastar_demo`，缺失时再选择 `SourceCode/sdk/verify/gros`。
- `modules-status` 和 `modules-sync` 是 GROS 专用命令；当 GROS 目录存在时按 GROS 处理模块状态和同步。
- 系统镜像、OTA、SD 包仍由 `SourceCode/project` 工程生成，应用构建由根 `build.sh` 统一调度。
- 设备无 USB，不生成 USB 工厂包；`--pack usb` 应作为非法参数失败。
- 工具链不固化进主仓，默认使用 `.toolchains/ssc305` 承载外部工具链仓拉取结果。
- 自动化构建不在脚本内隐式 checkout 或 pull；CI 或发布操作者应先同步主仓、应用仓和工具链，再执行 `build.sh self-check` 与构建。
- `SourceCode/sdk/verify/xcrz_sigmastar_demo` 是独立应用仓，应用库文件变更需要在应用仓单独提交和推送。

## Candidate Memory Actions

| Action | Target | Candidate |
| --- | --- | --- |
| write-to-memory-candidate | ~/.codex/memories/projects/pcr02_ssc305_compile.md | 记录统一构建入口、应用根目录优先级、无 USB 包、工具链目录、自动化同步边界。 |
| archive-only | session-wrap | 保留完整构建改造、验证命令、提交信息和风险说明。 |
| promote-to-agents | project AGENTS.md | 暂不建议；当前规则主要是项目事实，不是跨项目行为约束。 |

## Proposed Memory Draft

```markdown
# pcr02_ssc305_compile memory

## Project Facts

- 根目录 `build.sh` 是主仓唯一标准构建入口。
- 当前阶段应用根目录自动检测优先 `SourceCode/sdk/verify/xcrz_sigmastar_demo`，缺失时使用 `SourceCode/sdk/verify/gros`。
- GROS 过渡期中，`modules-status` 和 `modules-sync` 专用于 GROS 模块管理。
- 设备无 USB，不生成 USB 工厂包；只保留 SD、OTA、all 打包路径。
- 工具链默认放在主仓 `.toolchains/ssc305`，不提交进主仓。
- CI 和发布流程应先同步主仓、应用仓、工具链，再执行 `build.sh self-check` 和构建；`build.sh` 不隐式 checkout 或 pull。

## Validation Baseline

- 脚本语法：`bash -n build.sh` 和 `bash -n scripts/generate-build-info.sh`。
- 自检：`rtk bash build.sh self-check --allow-dirty --no-copy-nfs`。
- 完整构建：`rtk bash build.sh full --profile ap6303bh_512m_v20 --allow-dirty --no-copy-nfs`。
- USB 负向验证：`build.sh package --pack usb` 应失败。

## Open Risks

- `SourceCode/sdk/verify/xcrz_sigmastar_demo` 是独立仓，当前可能存在未提交库文件变更；需要单独检查、提交和推送。
- 后续若切换为 GROS 默认，应同步调整 `build.sh` 应用候选优先级和 README 当前阶段说明。
```

## Review Result

- 本文件只提供候选整理，不自动写入 `~/.codex/memories`。
- 若需要固化，请用户明确要求写入 memory candidate 或直接更新项目 memory 文件。
