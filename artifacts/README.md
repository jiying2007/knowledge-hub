# Artifacts

This directory stores manifests for external artifacts only.

Do not store raw SDK packages, logs, core dumps, release binaries, credentials, private keys, tokens, cookies, or other secrets here.
