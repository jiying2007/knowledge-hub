# Inbox

未分类输入、临时草稿和待处理材料放在这里。

规则：

- 默认不登记 registry。
- 不作为 active fact。
- 转长期资产时迁入 `notes/`、`projects/`、`domains/` 或 `sources/`。
- 工具恢复后运行 `rtk bash tools/knowledge-check.sh --dry-run --json --diagnostics`。
