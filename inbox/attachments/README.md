# 待分类附件

Obsidian、浏览器或人工新增的图片和附件先放在本目录。这里是 L0 临时输入，不是长期 artifact vault。

进入长期资产前必须确认：

- source、owner、敏感性和版权边界；
- 文件 size、sha256 和最终 disposition；
- 是否只保留 URI/hash，还是允许进入 `artifacts/vault/`；
- registry、manifest、回滚和验证要求。

不得在本目录保存 credential、private key、token、cookie、raw core、release binary 或未脱敏日志。
